# 0.5.2 功能回补与业务闭环计划

## 版本目标
0.5.2 不回滚 0.5.1 的减法方向，但修复“删得过头”的问题。原则是：

> 自动化不等于不可见；低频不等于删除；内部实现不常驻，但必要的查看、恢复、配置和异常处理必须完整。

## 一、信息架构
管理中心调整为 4 个一级入口：
1. 状态中心
2. 工作区
3. 本地记忆
4. 设置与诊断

其中“工作区”入口负责打开独立 Workspace Center，不在 Manager 里复制一套复杂管理页。

## 二、状态中心
恢复用户真正需要的状态：
- 当前工作区
- Runtime / 本地 MCP
- Tunnel
- OpenAI 上游
- ChatGPT MCP 挂载
- 当前任务
- 最近异常
- 启动/重启真实进度

正常时简洁；异常时出现直接处理入口。

### 任务详情
默认摘要：
- 目标
- 当前阶段
- 已运行时间
- 最近活动
- 当前命令（如有）
- 失败原因（如有）

技术详情折叠：
- Task ID
- Run ID
- Operation/Heartbeat 等内部字段

这些不再常驻主 UI。

## 三、工作区中心
保留现有：
- 最近工作区
- 搜索
- 切换
- 收藏
- 检查目录
- 删除失效记录
- 安全缓存清理

恢复并完善“额外授权目录”：
- 查看全部 authorizedRoots
- 显示存在 / 不存在 / 无访问权限
- 添加授权目录
- 单个取消授权
- 清空全部额外授权
- 主工作区不能被误当作额外授权删除
- 改动后广播刷新 Runtime/Workspace 状态

不恢复 8 类权限矩阵、路径/命令精细规则常驻 UI。

## 四、启动 / 重启真实进度
复用 RuntimeOrchestrator 的真实 progress/status 事件，不制造假百分比。

显示真实阶段：
1. 检查配置
2. 准备本地 Runtime
3. 启动 Runtime
4. 验证本地 MCP
5. 启动 Tunnel
6. 验证 OpenAI 通道
7. 验证 ChatGPT MCP 挂载
8. 完成

每个阶段展示：
- 等待 / 进行中 / 成功 / 失败
- 当前说明
- 真实耗时（可计算时）
- 失败原因
- 重试 / 一键诊断入口

## 五、配置教程
恢复“配置教程”，但不恢复旧 Guide checkbox 和百分比。

做成可随时重新打开的实际配置向导：
1. 选择工作区
2. 保存 Runtime API Key
3. 配置 Tunnel ID
4. 网络/代理自动检测
5. 启动服务
6. 自动验证 Runtime/Tunnel/OpenAI
7. 检查 ChatGPT MCP 挂载
8. 完成

特点：
- 每一步直接操作真实配置
- 已完成项目自动识别
- 可以跳到对应设置
- 失败给处理建议
- 配置完整后不强制弹出，但入口长期保留

## 六、Worktree 异常闭环
正常情况下隐藏 Worktree。

仅在以下情况显示“隔离修改待处理”：
- 有 active worktree
- 验证完成但未应用
- apply 冲突
- 中断后存在待处理修改

提供：
- 查看 Diff
- 应用回主工作区
- 丢弃隔离修改
- 冲突提示

复用现有：
- mcp:task-worktrees
- mcp:task-worktree-diff
- mcp:task-worktree-apply
- mcp:task-worktree-discard

不恢复旧的常驻 Worktree 控制台。

## 七、设置与诊断
保留常用设置：
- 主题
- 开机启动
- 自动启动服务
- 关闭窗口后台运行
- 桌面通知
- 通知声音

保留连接配置：
- Runtime API Key
- Tunnel ID
- 代理
- 网络检测

保留高级维护：
- MCP / Tunnel 端口
- 重置本地 Token
- 清除 ChatGPT 登录数据

诊断：
- Runtime
- Tunnel
- OpenAI
- ChatGPT MCP
- 一键诊断
- 一键修复
- 脱敏支持报告
- 折叠日志

## 八、继续删除的花架子
以下内容 0.5.2 不恢复：
- Agent 六模式常驻切换
- Tool Mode
- 8 类权限矩阵
- 路径/命令 ACL 常驻配置
- Context Pressure 数字
- 常驻 Heartbeat / Operation ID
- 常驻 Task ID
- Worktree Hash
- Guide checkbox/百分比
- Bridge 历史迁移提示
- 构建/性能独立导航页
- 审批收件箱

## 九、版本与测试
- package.json / package-lock.json -> 0.5.2
- Manager 展示 -> v0.5.2
- 新增 0.5.2 产品契约测试
- 保持 Runtime / MCP Schema 不变，除非实际修改公开协议
- npm run test 全绿
- npm run dist
- 验收 app.asar
- 输出安装包大小 + SHA-256

## 验收标准
1. 用户可以查看/删除授权目录。
2. 用户可以看到启动/重启当前正在进行哪一步，不是假百分比。
3. 配置教程可随时重新打开并真正完成配置。
4. 状态中心能判断服务、任务和异常状态。
5. Worktree 平时隐藏，只有需要处理时出现。
6. 仍然没有 0.5.0 那种权限矩阵、内部 Workflow、性能/构建控制台。
7. 全量测试通过后才生成 0.5.2 安装包。


## 十、2026-09-23 工作区中心 UI 修订

针对 0.5.2 首版工作区中心“工作区与授权目录纵向堆叠，搜索区被挤到下方、弹窗过长且视觉笨重”的问题，追加以下产品约束并已实施：

- 工作区中心使用二级导航：工作区 / 授权目录。
- 两个视图互斥显示，不允许同时纵向堆叠。
- 当前工作区摘要保持顶部，但改为紧凑横向摘要。
- 工作区视图固定包含搜索、检测、添加和列表。
- 授权目录视图固定包含添加授权、清空授权和授权列表。
- 主体采用固定内容槽，只有当前列表内部滚动，整个弹窗不做长页面滚动。
- 工作区弹窗默认尺寸调整为 820×620，最小 700×540。
- 缩小大块卡片、圆角、留白和字号，统一浅色/深色视觉层级。
- 底部仅保留状态与弱化后的缓存操作。
- Ctrl+F 自动切回工作区并聚焦搜索；Ctrl+1/2 可快速切换二级视图。
- 保留工作区切换、收藏、移除、检测、清理失效、授权添加、单个取消、清空授权和安全缓存清理逻辑。
- 新增 0.5.2 工作区 UI 防回归测试，禁止再次恢复成长页面堆叠。
