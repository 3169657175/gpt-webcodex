# 2026-09-24 ChatGPT 网页版更新后 MCP 助手使用体验与兼容性问题复盘

> 记录范围：本文件记录 2026-09-24 实际通过当前“网页 MCP 助手 / Coding Tools MCP”连续执行长任务时的真实体验、异常表现、可能根因和建议改造方向。  
> 当前工作区：D:\网页mcp助手  
> 当前项目版本：0.5.4  
> 说明：今天 ChatGPT 网页版确实出现了明显界面和交互变化，但“网页版更新导致全部问题”目前只能作为重要假设，不能直接当成已证实根因。更准确的判断是：**网页版更新可能成为触发器，暴露了本地 MCP 助手在任务状态、工具调度、进度反馈、错误恢复和网页兼容层上的脆弱点。**

## 一、这次实际使用下来最明显的问题

### 1. 长任务经常表现为“卡住”，但后台并不一定真的停止

这次连续执行较长开发任务时，多次出现下面这种体验：

- 已经发起工具调用；
- 页面只停留在“正在规划 / 正在执行”一类状态；
- 很长时间没有新的文字进度；
- 用户看起来只能判断“又卡了”；
- 实际上有时后台仍在运行，有时已经失败，但前台没有及时解释；
- 最终需要用户再次发“你是不是卡住了”，才重新汇报并继续。

这说明现在的“任务执行状态”和“用户可见进度”是脱节的。

问题不只是执行慢，而是缺少持续可见的心跳和阶段性反馈。

理想行为应该是：

- 进入长任务后立即显示当前阶段；
- 每完成一个阶段自动汇报一次；
- 超过 20～30 秒没有新输出时自动发一条简短状态；
- 如果工具仍在跑，应明确显示“仍在执行”；
- 如果后台已经失败，应立即显示失败原因和恢复动作；
- 不应该等用户再次追问以后才恢复沟通。

## 二、最严重的底层问题：agent_workflow 与 Git worktree

### 2. agent_workflow 多次因为 Git worktree 超时失败

这次出现过明确错误：

~~~text
Git worktree operation timed out.
~~~

更关键的是，我已经明确要求：

~~~text
不要创建新的 git worktree
直接在当前工作区修改
~~~

但高层 agent_workflow 仍然走到了涉及 worktree 的内部流程。

这说明当前高层 Agent 工作流存在一个结构性问题：

> 用户或模型已经选择“直接修改当前工作区”，但高层自动工作流仍可能私自进入 worktree 流程。

这会导致：

1. 不必要的等待；
2. Git 仓库较大时直接超时；
3. 工作流状态被标记 failed；
4. 实际修改还没开始，用户就看到任务像是卡死；
5. 后续只能绕过 agent_workflow，改用 exec_command 手动操作。

### 建议

增加一个真正有效的执行模式：

~~~text
workspace_mode = direct
workspace_mode = isolated_worktree
workspace_mode = auto
~~~

只要是 direct：

- 禁止创建 worktree；
- 禁止做隐式 branch/worktree 准备；
- 直接使用当前授权工作区；
- agent_workflow 内部所有子流程都必须继承该设置。

这个问题应该列为 P0。

## 三、任务状态机存在“失败状态粘住”的问题

### 3. 后台失败后，后续已经继续修改，但 task_control 仍然显示 failed

实际发生过：

1. agent_workflow 因 worktree 超时失败；
2. 随后通过 exec_command 继续正常修改；
3. 代码实际上已经被写入；
4. 但 task_control 仍保持 lifecycle = failed / status = failed；
5. 必须额外调用 resume 才能把任务恢复成 active。

这会让“真实执行状态”和“任务卡状态”不一致。

理想状态应该是：

- 某个子操作失败不等于整个任务永久失败；
- 如果后续成功恢复，应自动变成 recovering → resumed → running；
- 不应该要求模型手动记得调用 resume；
- UI 应区分“某一步失败”“整个任务不可恢复失败”“已自动恢复”。

推荐状态机：

~~~text
queued
running
waiting
recovering
running
verifying
completed
~~~

只有真正无法继续时才进入 failed。

## 四、工具调用成功/失败信息有时互相矛盾

### 4. 出现过 diagnostics 报错，但 execution exit_code 又是 0

实际遇到过类似：

~~~text
diagnostics:
EXECUTABLE_NOT_FOUND

execution:
exit_code: 0
ok: true
~~~

这类返回对模型很不友好。

必须额外判断：

- 到底命令有没有执行；
- 是 MCP 参数解析报错；
- 是 shell 包装层报错；
- 还是实际子进程成功。

建议把工具返回拆成明确字段，例如：

~~~text
transport_ok
command_started
command_exit_code
command_ok
diagnostic_warning
~~~

如果 executable 根本不存在，就不应该同时再返回 command ok 或 exit 0。

## 五、functions.exec 对复杂脚本的转义很脆弱

### 5. JavaScript → Python → TypeScript/TSX 多层字符串非常容易失败

这次为了修改较大的 TSX 文件，实际使用链路是：

~~~text
functions.exec
→ JavaScript
→ MCP exec_command
→ Python stdin
→ 写 TypeScript / TSX
~~~

其中出现过：

~~~text
Script error:
SyntaxError: Unexpected identifier '$'
~~~

根因不是业务代码，而是多层字符串中同时存在：

- JavaScript template literal；
- 插值符号；
- Python raw string；
- TSX / JSX；
- 正则表达式；
- Windows 反斜杠；
- PowerShell 引号。

任何一层转义不严谨，都会在工具包装层提前失败。

### 建议

Coding Tools MCP 应提供原生文件修改能力，例如：

~~~text
write_file(path, content)
apply_patch(path, patch)
replace_range(path, start_line, end_line, content)
~~~

避免模型通过 JS 字符串 → Python stdin → Shell quoting → PowerShell quoting → 文件内容 这种多层链路。

这是非常值得做的底层改进。

## 六、Windows 编码问题仍然是高风险点

### 6. UTF-8、CP936、PowerShell 默认编码混用会造成“看起来乱码”甚至真的写坏源码

这次实际输出中出现过类似：

~~~text
新对�?
发送消�?
正在思�?
~~~

其中一部分是终端输出解码使用 cp936 / utf-8-replace 导致的显示问题，不能单凭终端结果判断源文件已经损坏。

但更危险的是：

- PowerShell Set-Content；
- Windows 默认编码；
- Python UTF-8；
- Node UTF-8；
- MCP stdout 编码；

混在一起时，真的可能把源码写坏。

### 建议

整个 MCP 助手统一：

~~~text
source file read  = UTF-8
source file write = UTF-8 no BOM
stdout            = UTF-8
stderr            = UTF-8
PowerShell        = UTF-8
Python            = PYTHONUTF8=1
~~~

并尽量禁止使用默认编码的 Set-Content 修改源码。源码修改优先走 Python UTF-8 或原生 patch/write_file。

## 七、一次读取太多源码，会让 UI 看起来像“模型失去响应”

### 7. exec_command 经常返回 5 万～18 万字节的大块输出

这次为了分析聊天页，多次读取了非常大的源码片段。

问题包括：

1. MCP 工具需要等待命令完成；
2. 大结果需要从本地传到 ChatGPT；
3. 模型还要解析巨大结果；
4. ChatGPT 网页前台此时没有细粒度状态；
5. 用户只能看到长时间没有更新。

这会非常像“卡住”。

### 建议

默认使用：

~~~text
符号索引
→ 精准搜索
→ 读取函数附近 50～150 行
→ 修改
→ 验证
~~~

而不是一次 Get-Content 几百行甚至上千行。

MCP 还可以为 exec_command 增加智能输出上限、自动匹配提取和结构化摘要。

## 八、长任务进度汇报机制明显不够

### 8. “工具还在跑”与“应该给用户发消息”没有很好联动

这次后台操作曾返回 next_progress_report_in_seconds = 90。

从真实用户体验看，90 秒太长：

- 10 秒无反馈已经开始让人怀疑；
- 30 秒无反馈用户基本会认为卡住；
- 90 秒才要求汇报，体验明显不合格。

### 建议

按耗时分级：

- 0～8 秒：无需额外提示；
- 8～20 秒：显示 UI 状态；
- 20～30 秒：自动发送一条简短进度；
- 之后每 30～45 秒更新一次；
- 阶段切换时立即更新。

进度消息应该很短，例如：

> 正在修改聊天渲染层，已完成代码块组件；下一步接入输入框和思考按钮。

而不是每次输出一大段解释。

## 九、恢复机制仍然太依赖用户说“继续”

### 9. 可恢复错误后，系统应该自己继续，不应该停下来等用户

这次多次出现：

- worktree 超时；
- tool script 转义错误；
- command schema 参数错误；
- 某个命令不可用。

这些绝大多数都属于可恢复错误。

合理行为应该是：

~~~text
失败
→ 自动判断可恢复
→ 换一种实现方式
→ 继续执行
→ 告知用户已自动恢复
~~~

而不是：

~~~text
失败
→ 停止
→ 用户问“是不是卡住了”
→ 再继续
~~~

建议增加明确的自动恢复策略：

~~~text
GIT_TIMEOUT
→ 禁用 worktree
→ direct workspace

EXECUTABLE_NOT_FOUND
→ 查找替代 executable / shell

quoting error
→ 自动切换原生 file writer / patch API

command timeout
→ command_control 检查进程
→ 仍在运行则继续 poll
→ 已退出则进入下一步
~~~

## 十、ChatGPT 网页版更新可能带来的兼容性影响

### 10. 今天的异常很可能与网页版更新有关，但不能只归因于网页版

本项目本质上是在：

~~~text
ChatGPT 网页
↕
Electron WebView / DOM / 注入层
↕
MCP 工具
↕
本地 Runtime
~~~

之间做桥接。

因此 ChatGPT Web 一旦调整：

- React 组件树；
- DOM 结构；
- 消息发送按钮；
- 工具状态节点；
- 对话切换机制；
- 插件 / MCP 呈现机制；
- 流式消息更新方式；
- 页面内部路由；
- 输入框组件；
- Thinking / Work 等模式入口；
- 请求生命周期；

本地前端注入逻辑就可能出现兼容问题。

但是这次实际遇到的：

- Git worktree timeout；
- task state failed 粘住；
- 多层字符串转义；
- Windows 编码；
- 巨量 stdout；
- 进度汇报过慢；

这些问题本身并不是 OpenAI 网页版更新直接造成的。

更准确的说法应该是：

> ChatGPT 网页版更新可能改变了外层运行环境和交互节奏，使原本就存在的 MCP 任务调度和恢复问题被更频繁地暴露出来。

## 十一、建议增加“网页版兼容性检测层”

### 11. 不要等功能坏了以后才人工发现

每次启动应用时自动检测：

#### 基础 DOM

- 输入框是否存在；
- 发送按钮是否存在；
- 新聊天按钮是否可定位；
- 当前会话节点是否存在；
- 消息列表是否可读取；
- 工具调用状态节点是否仍能定位。

#### MCP 连续调用

自动冒烟测试：

~~~text
第 1 次工具调用
第 2 次工具调用
第 3 次工具调用
~~~

确认同一对话不会发生：

- MCP 消失；
- @MCP 不可发送；
- 工具仍可用但 UI 不可见；
- 第二条消息后失效。

#### 网页版本指纹

保存：

- 页面关键 selector；
- React / DOM 特征；
- 关键节点数量；
- bundle / build ID（如果可取得）。

当变化明显时提示：

> 检测到 ChatGPT 网页结构发生变化，当前 MCP 适配层已进入兼容模式。

然后自动运行兼容测试。

## 十二、建议的新版长任务执行内核

### A. 接到任务

~~~text
1. 确认当前工作区
2. 建立任务 ID
3. 输出 3～7 个执行阶段
4. 立即开始
~~~

### B. 执行中

每一个阶段：

~~~text
读取少量必要文件
→ 修改
→ 快速验证
→ 保存 checkpoint
→ 自动进入下一步
~~~

不要再采用：

~~~text
一次读取十几万字节
→ 思考很久
→ 一次大改
→ 最后才验证
~~~

### C. 错误发生

~~~text
可恢复错误
→ 自动修复
→ 自动继续
~~~

只有这些情况才真正停下来问用户：

- 需要额外文件权限；
- 需要管理员权限；
- 会删除重要数据；
- 用户需求存在互斥选择；
- 涉及无法自动判断的破坏性操作。

### D. 进度显示

UI 至少应该显示：

~~~text
当前任务：聊天页重构
阶段：3 / 7
正在执行：代码渲染层
已完成：2
最近成功：rich-message.tsx 已创建
当前命令：npm run typecheck
耗时：01:42
~~~

这样用户不会再靠“是不是卡住了”来判断状态。

## 十三、优先级建议

### P0：先解决稳定性

1. direct workspace 真正禁止 worktree；
2. 任务状态机自动从可恢复失败恢复；
3. 20～30 秒自动进度汇报；
4. MCP 断线自动重连并继续当前任务；
5. 工具错误自动 fallback；
6. Windows 全链路 UTF-8；
7. 增加原生 write_file / apply_patch，减少多层 shell 字符串。

### P1：解决 ChatGPT 网页兼容性

1. 网页 DOM/version 指纹；
2. 启动自动兼容检查；
3. 输入框/发送按钮/新聊天适配 fallback；
4. 工具调用节点 fallback selector；
5. 同一对话 3～5 次连续 MCP 调用自动测试；
6. 页面刷新、切会话、切账号后恢复测试。

### P2：改善速度

1. 默认精准读取源码；
2. 限制 stdout；
3. 大文件走索引；
4. 测试分层：syntax → targeted test → full test → build；
5. Docker build 只有真正需要时才执行。

### P3：改善用户观感

1. 长任务常驻小型进度条；
2. 显示“仍在运行 / 已恢复 / 正在重试”；
3. 不让失败任务卡长期停在 failed；
4. 后台任务完成时主动刷新聊天；
5. 长任务完成后给一份简洁变更摘要。

## 十四、我这次使用下来最希望优先修改的 8 个点

如果只选最关键的，我会优先做：

1. **direct workspace 真正禁止 worktree**；
2. **工具失败自动 fallback 后继续，不再等用户说“继续”**；
3. **30 秒以内必须有进度反馈**；
4. **task_control 状态和真实执行状态统一**；
5. **增加原生文件补丁/写入工具，减少 JS → Python → Shell 多层转义**；
6. **Windows UTF-8 全链路固定**；
7. **ChatGPT Web DOM 兼容性启动自检**；
8. **同一对话连续多轮 MCP 调用自动回归测试**。

## 十五、结论

这次体验说明，目前的 MCP 助手已经具备比较强的“实际干活能力”，真正的问题已经不再是“能不能调用工具”，而是：

> **长任务执行是否连续、状态是否可信、出错后是否能自己恢复、用户是否知道它现在到底在做什么。**

今天 ChatGPT 网页版更新很可能使这些问题表现得更加明显，但不应该只做网页 selector 层面的临时兼容。

下一阶段更应该把：

~~~text
网页兼容层
+
任务状态机
+
自动恢复
+
进度反馈
+
工具执行内核
~~~

作为一整套系统来升级。

最终目标应该是：

> 用户说一次“从第一步做到最后一步”，之后即使某条命令失败、网络瞬断、网页结构轻微变化，助手也能自己恢复并继续；用户始终能看到当前进度，不需要再反复发送“继续”“是不是卡住了”。

这应该成为后续 0.5.x / 0.6.0 稳定性升级的核心方向。
