# 0.5.5 稳定性收口与长任务体验修复计划

## 目标

把 0.5.4 实测中暴露的 Runtime、agent_workflow、Worktree、Windows 命令执行、任务恢复、进度汇报与发布链路问题一次性收口，发布可直接使用的 0.5.5。

## 已确认问题

1. agent_workflow 在 Windows Worktree 中执行 npm 时，带空格路径的 npm.CMD 引号处理错误，导致真实项目测试通过但隔离验证假失败。
2. agent_workflow 子步骤失败后，后续人工绕过并成功验证，任务状态仍可能停留 failed，需要额外 resume，恢复不够自动。
3. 后台工具被安全策略拦截、命令长时间运行或进入等待时，前台状态不够明确，用户容易误判为“卡死”。
4. 长任务进度汇报粒度不足；后台 operation、模型等待、本地命令、失败、恢复之间的状态文案仍不够连贯。
5. 测试失败的错误归因不够精确，环境/命令启动错误可能被误报为项目测试失败。
6. 需要重新验证 Python 便携 Runtime、Schema、连续 discovery/call、output_ref、Worktree、恢复、通知、构建安装包链路。
7. 发布前需确保 __pycache__ / .pyc 不进入安装资源、不污染 source root / Git。
8. 过程中发现的新稳定性问题直接加入本计划并继续迭代。

## 执行阶段

### 阶段 A：复现与根因定位
- 复现 Worktree + Windows npm.CMD 引号错误。
- 检查 processes / command runner / build verification / worktree 执行路径的命令构造。
- 检查 agent_workflow 对 verification failure、environment failure、blocked/denied 的分类。
- 检查 task lifecycle、background operation lifecycle、resume/recovery 的状态同步。
- 检查前台状态中心/任务条对 running、waiting-model、blocked、failed、recovering、completed 的映射。

### 阶段 B：Runtime 与 agent_workflow 修复
- 修复 Windows 可执行文件路径含空格时的命令启动。
- 保证 Worktree 中 npm/node/python 等命令与主工作区一致可执行。
- 区分“测试失败”和“测试命令未能启动”。
- 对可恢复的子步骤错误自动进入 recovering/resumed，而不是永久 failed。
- 完善 background operation 完成态与 task state 的一致性，避免出现 completed 文案却 error category=failed。
- 保证失败恢复不会破坏 Git index、主工作区和 Worktree 隔离。

### 阶段 C：长任务与进度汇报
- 为长任务提供可靠 heartbeat / progress event。
- 安全策略拦截、等待授权、等待模型、等待命令、后台执行、自动恢复分别显示真实状态。
- 避免长时间无 UI 更新；在没有新日志时也显示“仍在运行/等待什么”。
- 用户可见文案中文化，技术 ID 默认折叠。
- 保证工具调用中断后重新连接仍能恢复到当前任务。

### 阶段 D：测试与稳定性回归
- Python Runtime 全量 pytest。
- Node/Electron 全量测试。
- Schema contract 校验。
- 连续 MCP discovery/call 多轮。
- Worktree 隔离执行 npm test。
- 命令 stdout/stderr/output_ref 大输出分页与编码。
- task fail -> recover -> complete。
- Runtime/Tunnel 分层恢复。
- 快速 soak；若被安全策略阻止，改用等价且受支持的稳定性测试，不把拦截误报为代码失败。
- source root clean / pycache / pyc 检查。

### 阶段 E：发布 0.5.5
- 更新 package/runtime 版本与发布说明。
- 全量测试全部通过后执行 npm run dist。
- 验证 dist 安装包存在、版本正确、资源无 pycache/pyc。
- 输出 0.5.5 安装包与最终验收结论。

## 动态问题清单

> 执行过程中发现的新问题持续追加到这里，并在发布前全部关闭或明确记录为非阻塞项。

## 本轮执行结果（2026-09-24）

- 已完成 Windows + Worktree 下 npm.CMD / 带空格可执行路径修复。
- 已完成 agent_workflow 失败状态、自动恢复、heartbeat、终态一致性修复。
- 已完成状态中心长任务进度展示、最近活动、失败原因、恢复状态与中文文案修复。
- 状态查询临时失败时不再清空当前任务，改为保留最后一次已知进度并显示“状态待确认”。
- command_control 的 poll/read 已按只读查询处理；task_control compact 查询已覆盖 running / queued。
- Runtime 升级到 0.5.3，MCP Tool Schema 升级到 v9 / 9 tools。
- Schema hash：cb44f23fd265c4a7d10c801ca9ef88ee0fa4228b7a73151b02ff693f23757949。
- 新增 Runtime 0.5.5 回归 5 / 5 通过。
- 进度/状态专项回归 48 / 48 通过。
- Python Runtime 全量 258 / 258 通过。
- Node / Electron 全量 161 / 161 通过。
- source root clean 通过。
- Windows 进程树测试在 ResourceWarning 视为错误时 1 / 1 通过；两个未关闭 PIPE 已修复。
- 发布说明已生成：docs/RELEASE_NOTES_0.5.5.md。
- 剩余步骤：执行 npm run dist，校验 0.5.5 安装包版本、大小、SHA-256 与安装资源缓存。

### 动态发现并关闭的问题

- 便携 Python 没有 pytest：确认正式测试入口为 scripts/run-python-tests.js，避免把测试入口错误误报为项目失败。
- 当前 ChatGPT MCP 连接可能缓存旧 Schema：通过提升 Runtime/Schema 身份，使桌面端能够识别旧 Runtime 并重新发现工具。
- 一行小修改也可能触发大 Worktree：保留安全默认 auto，同时在 agent_workflow Schema 中明确 isolation=off 的轻量直改用途。
- 状态轮询偶发被安全层拦截：补只读分类与前端状态保留，不再让任务“凭空消失”。
- 专项测试残留 0.5.4 版本断言：已更新并重新通过。

## 最终发布验收

- [x] npm run dist 完成，exit 0。
- [x] 安装包：D:\网页mcp助手\dist\web-mcp-assistant-setup-0.5.5.exe
- [x] 安装包大小：126,276,801 bytes（120.43 MB）。
- [x] FileVersion：0.5.5。
- [x] ProductVersion：0.5.5。
- [x] SHA-256：53C336E73D52CBD29F5C8EAA1F4C5B70AC0C6F0D2FC76BD92A323723D04D4C56
- [x] dist\win-unpacked 中 Coding Tools MCP 资源的 __pycache__/.pyc 数量：0。
- [x] 0.5.5 发布完成。



### 动态发现：状态查询污染与 command_control 收尾
- 发现只读 command_control poll/read 查询旧 Session 失败时会覆盖当前任务 failure，造成“任务正常运行却显示失败”。
- 发现 command_control poll 返回真实命令最终退出码后，没有像 write_stdin 一样同步收尾 current_command。
- 修复要求：只读状态查询不得改变任务成败；对当前 Session 的 poll/write 必须同步真实命令状态；kill 必须正确结束 current_command；增加回归测试锁定。


## 最终验收结果

- Desktop：0.5.5
- Coding Tools MCP Runtime：0.5.3
- MCP Tool Schema：v9 / 9 tools
- Schema hash：cb44f23fd265c4a7d10c801ca9ef88ee0fa4228b7a73151b02ff693f23757949
- Python 全量测试：261 / 261 通过
- Node / Electron 全量测试：161 / 161 通过
- source root clean：通过
- npm run dist：exit 0
- 最终安装包：dist/web-mcp-assistant-setup-0.5.5.exe
- 安装包大小：126277215 bytes（120.43 MiB）
- SHA-256：79DBDB8492DCE326F3866A6637D52C821D3394F9AC793CD687B4069891E5B89D
- 打包资源 __pycache__ / .pyc：0
- 打包后 Runtime Windows structured_process 烟测：npm --version -> 11.12.1，exit 0
- 发布说明：docs/RELEASE_NOTES_0.5.5.md

### 最终结论

0.5.5 已完成本轮计划中的 Runtime、agent_workflow、Worktree/Windows 命令启动、失败恢复、command_control 状态同步、长任务 heartbeat、状态查询失败保留进度、Schema 身份升级与发布构建收口。最终安装包已生成并通过发布前验收。
