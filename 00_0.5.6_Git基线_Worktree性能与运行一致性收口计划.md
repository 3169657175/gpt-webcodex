# 0.5.6 Git 基线、Worktree 性能与运行一致性收口计划

## 目标

0.5.6 不以新增表面功能为主，而是彻底解决 0.5.5 验收中暴露出的工程基线、Worktree 性能、Schema 漂移、任务终态一致性、Windows 中文输出和长任务准备期反馈问题，并最终形成可验证、可发布、Git 基线干净的 0.5.6。

## 阶段 1：重建可信 0.5.5 Git 基线

1. 分类当前 Git 修改、删除和未跟踪文件。
2. 区分正式源码、正式测试、历史文档、缓存/生成物、可疑临时文件。
3. 完善 .gitignore。
4. 清除 Git 已跟踪的 __pycache__ / *.pyc 等生成物，不删除正式源码。
5. 完整运行 0.5.5 测试。
6. 将当前已经验证可运行的 0.5.5 正式状态提交为新的可信 baseline。
7. 验证 git status 基本 clean。

## 阶段 2：优化 Worktree 与 agent_workflow 启动性能

1. 纯 diagnose/read-only/只读测试场景不创建隔离 Worktree。
2. 真正写代码、重构、patch/create/delete 时保持安全隔离。
3. 为 clean baseline 下的 Worktree 建立快速路径。
4. 避免同一 workspace fingerprint 重复生成昂贵快照。
5. 增加 Worktree 生命周期清理规则。
6. 用真实耗时对比优化前后启动性能。

## 阶段 3：Schema 单一来源与旧会话漂移检测

1. 统一服务端工具声明、schema-contract、MCP tools/list 的 Source of Truth。
2. 增加 Schema generation/hash 一致性检测。
3. Runtime 启动时验证实际 tools/list。
4. 桌面端能区分 Runtime 已升级但 ChatGPT 当前会话仍使用旧 Schema 的情况。
5. 对旧会话无法热刷新时明确提示新建聊天，而不是误报为工具故障。
6. 添加 Schema 回归测试。

## 阶段 4：任务/命令终态一致性

1. 建立统一 execution finalize 路径。
2. task、operation、current_command、last_command、event log 共享一致终态。
3. completed/failed/timed_out/cancelled 均做回归。
4. 禁止 workflow finalize 使用旧 command snapshot 覆盖最新 execution 结果。

## 阶段 5：Windows 中文输出编码统一

1. Python 执行环境优先统一 UTF-8。
2. Node/PowerShell 执行环境统一 UTF-8 可预期输出。
3. decoder 保留 UTF-8 / UTF-16 / CP936 兼容兜底。
4. 添加中文 stdout/stderr、分块多字节字符、CP936 fallback 测试。
5. 验收实际 npm test 输出不再出现乱码。

## 阶段 6：长任务准备期可见性

1. 将准备阶段拆成真实步骤：Git 检查、隔离判断、Worktree 创建、快照、依赖/命令准备、测试/构建。
2. 超过数秒的准备步骤立即更新 task progress。
3. 记录每阶段耗时。
4. 避免用户只看到 Run complete agent workflow 长时间不变化。

## 阶段 7：Worktree 生命周期与安全 GC

1. completed 且 clean、无待应用修改的 Worktree 自动清理。
2. 已安全应用回主工作区的 Worktree 自动清理。
3. 存在未应用修改的 Worktree 永不自动删除，只标记待恢复。
4. 清理过期本地 run branch 与无用 operation 记录。
5. 防止 .coding-tools 长期膨胀。

## 阶段 8：发布流程工程化

1. 版本更新至 0.5.6。
2. 完整测试。
3. 发布构建。
4. 验证安装包、版本、哈希。
5. Git 状态检查。
6. 提交 0.5.6 正式代码并建立 tag。
7. 发布后再次验证 Git baseline clean。
8. 将“发布完成必须同步 Git 基线”写入 AGENTS/发布规则。

## 阶段 9：最终专项验收

- 同一聊天连续 20+ MCP 调用。
- 后台 agent_workflow 同时 task_control/command_control。
- 短 diagnose 不创建 Worktree。
- 写操作仍安全隔离。
- clean repo 下 Worktree 启动显著缩短。
- Runtime Schema 与实际 tools/list 一致。
- 任务终态字段一致。
- 中文输出无乱码。
- MCP/Tunnel 恢复路径正常。
- npm test 全绿。
- npm run dist 成功。
- 0.5.6 安装包完成最终发布验收。

## 阶段 6B：ChatGPT stream recovery polling timeout 专项修复（新增阻断项）

该问题已在 0.5.6 开发过程中连续复现，不再按偶发网页错误处理。

目标：
1. 明确区分 ChatGPT 网页回复流恢复超时与本地 MCP/任务失败。
2. 识别 `ChatGPT stream recovery polling timed out`，记录为 `page-stream-recovery-timeout`。
3. 网页流恢复失败时不得自动刷新页面、自动点击重试或重放 MCP 工具，避免重复副作用。
4. 本地 task、operation、command、worktree 状态必须继续持久化，可在网页流恢复后继续汇报。
5. 顶部进度提示显示“网页回复恢复超时，本地任务状态仍已保存”，而不是误报“MCP 崩溃”。
6. 修复连续恢复计数无限累积：成功的 follow-up recovery command 后清零连续恢复计数；未知副作用恢复证据仍保留。
7. 增加连续两次 stream recovery timeout、长回复、长工具链、MCP Session 重连后的回归测试。
8. 最终 0.5.6 dogfood 必须真实验证：网页流报错后，本地任务不中断；用户继续发消息后能从持久状态继续，不需要重新执行已经完成的步骤。

## 2026-09-24 最终执行状态

已完成：
- Desktop 版本 0.5.6。
- Python Runtime 版本 0.5.6。
- MCP Schema v10，contract hash cb44f23fd265c4a7d10c801ca9ef88ee0fa4228b7a73151b02ff693f23757949，9 tools。
- Worktree 快照兼容历史 tracked cache。
- diagnose / artifact-only build_release 自动跳过不必要 Worktree。
- Worktree 准备耗时反馈、无差异安全自动回收。
- task / current_command / last_command 终态同步。
- follow-up command 成功后连续恢复计数收口。
- Windows Python/Runtime UTF-8 主路径。
- ChatGPT stream recovery polling timed out 专项识别与安全提示，不自动重试/刷新/重放工具。
- Schema 代际变化提示旧聊天可能需要新建聊天刷新参数。
- 全量 npm run test 最终通过：242 行，exit 0，OK。
- npm run dist 成功。
- 安装包：dist/web-mcp-assistant-setup-0.5.6.exe
- 安装包大小：126279624 bytes。
- SHA-256：DCAA6A364A07420C45AD2F1F3A043DC6865C3F10AA482ED68729094ADF78EB62
- latest.yml、app.asar、打包内 Runtime 与 schema-contract 均验证为 0.5.6 / Schema v10。
- 历史 Git tracked Python cache 共 87 个已从 Git index 移除；git ls-files 中 tracked pyc 剩余 0。
- git diff --check 与 git diff --cached --check 均通过。

唯一未完成的工程收尾：
- git add / 正式 baseline commit / v0.5.6 tag / 最终 clean status。
- 原因不是代码或 Git 错误，而是当前网页 MCP 助手本地安全策略要求对 Git index 写入做 LOCAL_APPROVAL_REQUIRED，而当前 ChatGPT 客户端不支持 MCP elicitation 弹出本地批准框。
- 不允许绕过该本地权限保护。用户在“工作区与权限”允许 Git index/提交操作后，应继续执行：显式暂存全部已审计正式文件 → commit 0.5.6 baseline → tag v0.5.6 → final git status clean。

